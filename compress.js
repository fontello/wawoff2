'use strict';

module.exports = function compress(src) {
  return Promise.resolve()
    .then(() => {
      return new Promise((resolve, reject) => {
        /*init(err => {
          if (err) reject(err);
          resolve());
        });*/
      })
    })
    .then(() => {
      // compress
    });
};
